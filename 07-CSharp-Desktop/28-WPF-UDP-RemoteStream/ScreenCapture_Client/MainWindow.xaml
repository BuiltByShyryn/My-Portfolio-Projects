<Window x:Class="ScreenCapture_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenCapture_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
          
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="IP Address: " VerticalAlignment="Center"/>
            <TextBox x:Name="txtIp" Text="127.0.0.1" Width="100" Margin="5,0"/>

            <TextBlock Text="Interval (ms): " VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox x:Name="txtInterval" Text="1000" Width="50" Margin="5,0"/>

            <Button x:Name="btnStart" Content="Start Capture" Click="btnStart_Click" Width="100" Margin="10,0"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
            <Image x:Name="imgDisplay" Stretch="Uniform"/>
        </Border>
    </Grid>
</Window>
