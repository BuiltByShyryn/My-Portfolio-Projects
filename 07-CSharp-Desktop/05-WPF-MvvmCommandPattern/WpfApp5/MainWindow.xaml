<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        
        <Label FontSize="150" 
               HorizontalAlignment="Center" 
               VerticalAlignment="Center" 
               Content="{Binding Count}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding Click}" CommandParameter="1" Padding="15">+1</Button>
            <Button Command="{Binding Click}" CommandParameter="2" Padding="15">+2</Button>
            <Button Command="{Binding Click}" CommandParameter="3" Padding="15">+3</Button>
            <Button Command="{Binding Click}" CommandParameter="4" Padding="15">+4</Button>
            <Button Command="{Binding Click}" CommandParameter="5" Padding="15">+5</Button>
        </StackPanel>
    </Grid>
</Window>
