<Window x:Class="WpfApp7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notes" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBox Width="150" Margin="5" Name="TitleBox"/>
            <TextBox Width="200" Margin="5" Name="TextBoxNote"/>
            <DatePicker Width="150" Margin="5" Name="DeadlinePicker"/>
            <Button Content="Добавить" Margin="5" Click="AddNote_Click"/>
            <Button Content="Удалить" Margin="5" Click="DeleteNote_Click"/>
        </StackPanel>

        <ListView Grid.Row="1" Name="NotesListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Text" DisplayMemberBinding="{Binding Text}" Width="350"/>
                    <GridViewColumn Header="Deadline" DisplayMemberBinding="{Binding Deadline}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
